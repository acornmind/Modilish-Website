// Shared, browser-safe constants — no server-only imports (fs, etc.) here,
// since this is used by both server code and client components.

/** Default "آستانه هشدار موجودی" — docs/admin-spec.md §4.3.2. */
export const LOW_STOCK_THRESHOLD_METERS = 2;
